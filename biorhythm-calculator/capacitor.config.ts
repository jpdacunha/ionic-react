import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.example.byorythm',
  appName: 'Biorythm Calculator',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
